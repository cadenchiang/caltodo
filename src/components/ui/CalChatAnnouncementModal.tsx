"use client";

/**
 * CalChat announcement modal — DISABLED.
 * Returns null early to prevent the modal from appearing for any users.
 * Kept as a no-op component so existing imports don't break.
 */
export default function CalChatAnnouncementModal() {
  return null;
}
